import java.util.List;

public interface Search<V> {
    List<V> SearchPath(V source, V dest); //честно говоря не совсем понял
    // какой я класс должен был сделать из Search, сделал интерфейс, вроде логично
}
